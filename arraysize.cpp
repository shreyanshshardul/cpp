#include<bits/stdc++.h>
using namespace std;
int main(){
    int a[]={1,2,3,4,5};
    //for calculating total space occupied by array
    cout<<sizeof(a)<<endl;
    //for calculating array size or no. of element in array
    cout<<sizeof(a)/sizeof(a[1]);
    return 0;
}